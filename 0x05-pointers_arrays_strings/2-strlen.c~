#include "holberton.h"
/**
 * strlen - returns the length of a string
 * @s: pointer to a char
 *
 * Return: int, the length
 */

int _strlen(char *s)
{
	int count = 0;

	while (s != '\0')
	{
		count++;
		s++;
	}
	return (count);
}
