#include "holberton.h"
#include <stdio.h>
/**
 * main - multiples two interger
 * 
 * Return: result (Success)
 */

int main(void)
{
	int i;

	i = 1;
	while (i <= 100)
	{
		if (i % 3 == 0 && i % 5 == 0)
			printf("Fizz Buzz");
		else if (i % 5 == 0)
			printf("Buzz");
		else if ( i % 3 == 0)
			printf("Fizz");
		else
			printf("%d", i);
		printf(" ");
		i++;
	}
	return (0);
}

